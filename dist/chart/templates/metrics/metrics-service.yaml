{{- if .Values.metrics.enable }}
apiVersion: v1
kind: Service
metadata:
  name: datamover-operator-controller-manager-metrics-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chart.labels" . | nindent 4 }}
    control-plane: controller-manager
spec:
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    control-plane: controller-manager
{{- end }}
